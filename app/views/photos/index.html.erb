<h1>Gallery</h1>
<div>
	<% @photos.each do |photo| %>
		<div style="display:inline-block">
			<a href= "<%= photo %>" >
				<%= image_tag photo, size: "300x300", alt: "img"%>
			</a>
		</div>
	<% end %>
</div>