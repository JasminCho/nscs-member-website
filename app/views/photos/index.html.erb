<h1>Gallery</h1>
<div class="gallery-container">
	<% @photos.each do |photo| %>
		<div class="gallery-photo" style="display:inline-block">
			<a href= "<%= photo %>" >
				<%= image_tag photo, size: "300x300", alt: "img"%>
			</a>
		</div>
	<% end %>
</div>
